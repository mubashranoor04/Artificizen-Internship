# Day 5 – OOP Part 2 + Most-Used Practical Python

## Topics Covered

* Inheritance (Single & Multiple Inheritance)
* `super()` Function
* Polymorphism (Method Overriding & Duck Typing)
* Abstraction using Abstract Base Classes (`abc` module)
* Magic (Dunder) Methods

  * `__str__`
  * `__repr__`
  * `__len__`
  * `__eq__`
  * `__add__`
* Exception Handling

  * `try`
  * `except`
  * `else`
  * `finally`
  * Custom Exceptions
* File Handling

  * Reading and Writing Files
  * `with` Statement (Context Managers)
* Working with JSON

  * `json.dump()`
  * `json.load()`
  * `json.dumps()`
  * `json.loads()`
* Practical Python

  * Virtual Environments (`venv`)
  * `pip`
  * `requirements.txt`
  * Basic Type Hints

---

## Practice Tasks

### Question 1

Implemented an `Animal` base class with `Dog` and `Cat` subclasses demonstrating inheritance and method overriding.

### Question 2

Created a custom `InsufficientBalanceError` exception and integrated it into the `BankAccount` class to prevent withdrawals beyond the available balance.

### Question 3

Built a file handling program that reads a text file and reports the total number of lines, words, and characters.

### Question 4

Stored a list of student records in a JSON file and loaded the data back into Python.

### Question 5

Implemented the `__str__()` and `__repr__()` magic methods in a `Point` class for readable object representation.

### Question 6

Used `try`, `except`, `else`, and `finally` to handle multiple exception types including:

* `ValueError`
* `ZeroDivisionError`
* `FileNotFoundError`

---

## Learning Outcome

Through today's practice, I gained hands-on experience with advanced Object-Oriented Programming concepts, custom exception handling, file operations, JSON data handling, and commonly used Python features that are widely used in real-world software development.

---
